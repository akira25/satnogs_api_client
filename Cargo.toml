[workspace]
resolver = "3"
members = [
    "satnogs_apiclient",
    "satnogs_pyapi_client"
]

# [profile.minsize]
[profile.release]
# inherits = "release"
strip = true  # Automatically strip symbols from the binary.
opt-level = "z"  # Optimize for size.
lto = true
codegen-units = 1
panic = "abort"
